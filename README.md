# YCL Public Library
一个 JavaScript 公共函数库，包含常用工具函数，使用 Rollup 打包。入口文件为 `src/index.js`，打包配置为 `rollup.config.js`。
## 构建
```bash
git clone https://github.com/YCL0609/YCL-Public-library.git
cd YCL-Public-library
npm i
npm run build
```